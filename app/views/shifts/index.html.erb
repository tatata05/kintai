<%= month_calendar events: @shifts do |date, shifts| %>
  <%= date.day %>
  <% shifts.each do |shift| %>
    <div>
      <%= shift.employee.name %>
      <%= shift.start_time %>
    </div>
  <% end %>
<% end %>