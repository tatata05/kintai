<% provide(:title, "employee_show") %>
<div class="top">
  <h2>従業員詳細</h2>
  <% if @employee.blank? %>
    <p>該当するアカウントは存在しません</p>
  <% else %>
    <%= render "shared/employee" %>
    <%= link_to "取消", @employee, method: :delete, data: {confirm: "本当に削除してよろしいですか？"}, class: "btn btn-primary" %>
  <% end %>
</div>
